# Quickstart

```{warning}
Not yet written. This page will walk from a spreadsheet of runs to a calibrated
forecast in under ten minutes, using a published dataset — not a synthetic one.

It is deliberately held back until real-data validation lands, because writing
it against simulator output would mean writing it twice and would misrepresent
what the tool has been shown to do. See [Limitations](limitations.md).
```

Until then, [Install](install.md) and the [API reference](api/index.md) are the
useful entry points.
